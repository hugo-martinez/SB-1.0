<%= form_for(dispo_teach) do |f| %>
  <% if dispo_teach.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dispo_teach.errors.count, "error") %> prohibited this dispo_teach from being saved:</h2>

      <ul>
      <% dispo_teach.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :teach_id %>
    <%= f.text_field :teach_id %>
  </div>

  <div class="field">
    <%= f.label :integer %>
    <%= f.text_field :integer %>
  </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.date_select :date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
