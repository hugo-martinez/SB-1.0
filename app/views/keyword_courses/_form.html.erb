<%= form_for(keyword_course) do |f| %>
  <% if keyword_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(keyword_course.errors.count, "error") %> prohibited this keyword_course from being saved:</h2>

      <ul>
      <% keyword_course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %>
    <%= f.number_field :course_id %>
  </div>

  <div class="field">
    <%= f.label :keyword_id %>
    <%= f.number_field :keyword_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
