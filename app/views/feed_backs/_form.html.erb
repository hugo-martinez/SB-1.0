<%= form_for(feed_back) do |f| %>
  <% if feed_back.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(feed_back.errors.count, "error") %> prohibited this feed_back from being saved:</h2>

      <ul>
      <% feed_back.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lessons_id %>
    <%= f.number_field :lessons_id %>
  </div>

  <div class="field">
    <%= f.label :feed_backer_id %>
    <%= f.number_field :feed_backer_id %>
  </div>

  <div class="field">
    <%= f.label :note %>
    <%= f.number_field :note %>
  </div>

  <div class="field">
    <%= f.label :comment %>
    <%= f.text_field :comment %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
