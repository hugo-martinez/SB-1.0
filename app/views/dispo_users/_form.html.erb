<%= form_for(dispo_user) do |f| %>
  <% if dispo_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dispo_user.errors.count, "error") %> prohibited this dispo_user from being saved:</h2>

      <ul>
      <% dispo_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :dispo %>
    <%= f.date_select :dispo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
